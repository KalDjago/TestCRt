# TAM CRt Analytics V3.1

Fixes over V3:
- True multi-select Master UIO upload, processed sequentially
- True multi-select Achievement CSV upload, processed sequentially
- Population Filter restored: All, Area, Dealer, Outlet
- Master Outlet enrichment and persistent Data Bank
- Light/dark mode

Railway volume: mount `/app/data`, set `DB_PATH=/app/data/crt_v31.db`.
